{
	order rate_limit before basicauth
}

:80 {
	reverse_proxy :4000

	rate_limit {
		zone dynamic {
			key    "{ http.request.remote.host }"
			events 2
			window 1s
		}
	}
	log {
		format console
	}
}
